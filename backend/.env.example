DATABASE_URL=postgres://postgres:postgres@localhost:9432/twodo?sslmode=disable
JWT_SECRET=change-me-to-a-long-random-string
PORT=9000
WORKER_INTERVAL_MINUTES=5
EXPO_ACCESS_TOKEN=

# Comma-separated list of allowed Google OAuth client IDs (iOS, Android, web).
# Find these in Google Cloud Console -> APIs & Services -> Credentials.
GOOGLE_CLIENT_IDS=

# Resend transactional email - used to send 6-digit sign-in codes.
RESEND_API_KEY=
EMAIL_FROM=2Do <onboarding@resend.dev>
